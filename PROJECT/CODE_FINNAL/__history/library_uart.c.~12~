//! library depedences
#include <stdio.h>
#include <stdint.h>
#include <stdbool.h>

#define UART_BUFFER_SIZE 128

typedef struct {
    uint8_t buffer[UART_BUFFER_SIZE];
    volatile uint16_t head;
    volatile uint16_t tail;
} buffer;

//!UART_Buffer uart_tx_buffer = { .head = 0, .tail = 0 };
//!UART_Buffer uart_rx_buffer = { .head = 0, .tail = 0 };


