#define maxLevel 5

usi8 countLevel = 0;

void menu(){
   if (tick_btn(true,bt0)) countLevel++;
   if (countLevel >= maxLevel) countLevel = 0;
   
   lcd_setCursor(0,0);
   lcd_data(countLevel + 0x30);
}
